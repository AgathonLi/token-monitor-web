# 2026-09-11

## 额度卡对齐桌面字段

对照桌面 Limits 卡：Codex 的 Luna Reserve / reset credits，OpenCode 的 Monthly。不改 fork。

- 大改前快照：`versions/2026-09-11-pre-limits-fields.html`
- additional 窗口用 `label`；`gpt-reserve` 显示为 Luna Reserve
- `billing`/`monthly` 显示为 Monthly，整行
- 读取 `resetCredits`（`N resets` + 最多三条到期时间）
- 额度页画出 additional；主页仍隐藏 Codex additional（与桌面 compact 一致）
- 主页不再只切 2 个窗口，补进度条和 `Updated · source`

未 push。

## OpenCode 色与 logo

桌面 `clientColors.opencode` 是 `#000000`，图标是窗格 SVG（`assets/icons/opencode.svg`），网页误用琥珀色 + 字母 O。改为黑色进度条 + 同一 path 的窗格标。

未 push。

## 各家 logo 对齐桌面 SVG

额度/工具/模型上的字母色块改为桌面 `assets/icons/<id>.svg` 的 currentColor 窗格标，颜色跟 `clientColors`（如 OpenCode `#000000`、Kilo `#F8F676`、Zed `#4173e7`）。快照：`versions/2026-09-11-pre-provider-icons.html`。

未 push。

## 模型页 logo

模型名经 `modelVendorFor` 映射到提供方 SVG（与桌面 usageCharts 一致），模型页与主页模型行不再用首字母色块。

未 push。
