# 2026-09-13

## Grok 周额度被标成 Monthly

Grok 采集始终发 `kind: billing`，周/月写在 `label`（Weekly / Monthly / Billing）。网页把所有 billing 画成 Monthly，周账期会标错。不改 fork。

- 大改前快照：`versions/2026-09-13-pre-grok-billing-label.html`
- 非 additional 的 billing：有 `label` 用 `label`，否则 Monthly（与桌面 `label || 'Monthly'` 一致）

