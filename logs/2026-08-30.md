# 2026-08-30 开发日志

会话从 Hermes Scratch 迁入本项目。此前在同一对话里完成 Cloudflare Hub 部署，再做网页看板。

## 背景（不在本仓库）

- 桌面应用：Token Monitor；Hub：Cloudflare Worker `token-monitor-hub`
- Fork：`AgathonLi/token-monitor`（上游 `Javis603/token-monitor`），Workers Builds 根目录 `worker/`
- 浏览器直接打开 Hub 根路径会 `401 unauthorized`，这是密钥已生效；`503 secret_required` 才是线上没读到 Secret
- 一键 Deploy to Cloudflare 会孤立副本、不跟上游；正确做法是 Builds 连 fork + Sync fork

## 本仓库决定

- 方案 B：独立单页 HTML，不改 fork
- 密钥存在浏览器 `localStorage`（`tm-dash-v1`），用 `Authorization` 头，不用 `?secret=` 当默认
- 界面按用户提供的桌面小组件截图做成浅色：主页 / 工具 / 额度 / 模型 / 趋势，DAY/MONTH/TOTAL
- 电脑与手机同一套布局，桌面居中约 430px，避免两套视觉分叉后再对不齐截图

## 已修问题

- `.gate { display: grid }` 盖过 `hidden`，连上后登录卡仍铺满 → 增加 `[hidden] { display: none !important }`
- Python `http.server` 日志里的 `favicon.ico` 404 与 `GET / 304` 不代表 Hub 失败；Hub 请求不经过这台本地服务器
- 打开方式必须是 `http://127.0.0.1:8765/`，改代码后要 Ctrl+F5

## 当前稿

- `index.html`：小组件风格看板（从 Scratch 迁入）
- `versions/2026-08-30-widget-ui.html`：同内容冻结快照

Scratch 里被覆盖的第一版深色 Monitor 稿未单独留档。

## 许可证与可见性（同日后续）

- 仓库按 **public** 维护，许可证 **MIT**，与上游 Token Monitor 对齐
- `LICENSE` 双版权：Agathon（本网页客户端）+ Javis（Token Monitor）
- README / AGENTS.md 改为：禁止冒充官方、禁止提交真实 Hub 子域；允许 git，推送默认 public
- 本地 git 已存在；公开远程已推送（当时 `a49d745`）

## 未决（当时）

- 计划名、OAuth/Web 等若 Hub JSON 没有则空白（额度页会读 `planName` / `source`，没有仍留空）
- 尚未放到 Cloudflare Pages / GitHub Pages（本次明确不做）
- 本地 git / GitHub public 远程：当时已推送 `a49d745`；当前 `main` 为 `34a14fe` 后再加本日志所述改动（尚未 push）
- README 改为给人看的 EN + `README.zh-CN.md`；维护约定留在 `AGENTS.md`

## 同日：对照上游后的文档 + 看板

对照 `Javis603/token-monitor`（Worker 源码含 `/api/history`、`devices[]`、`periodWindows.timeZone`）。不改 fork，不开 Pages。

- 大改前快照：`versions/2026-08-30-pre-hub-views.html`
- README EN/中：补回被截断的 `Authorization: Bearer` 句；视图表增加设备
- `AGENTS.md`：仓库已是 public git，红线改为未确认不要 push / force-push / 改 remote / 开 Pages
- `index.html`：标题改为 Token Monitor Web；增加设备视图；热力图/连续天数按 Hub 时区（无则用浏览器时区）的日历日，不用 UTC `toISOString()`；额度紧凑视图跳过 `additional` 窗口并识别 credits；补一批工具色板

## 同日：PC 自适应看板

改掉「电脑也锁 430px」。≥860px 为顶栏选项卡 + 主页两列；窄屏仍为组件栏。不开 Pages。

- 大改前快照：`versions/2026-08-30-pre-wide-layout.html`

## 同日：统一风格 + 排序 + 中英 + 同步时间

- 快照：`versions/2026-08-30-pre-i18n-sort-asof.html`
- 去掉手机悬浮圆角框与底栏，顶栏选项卡手机/电脑共用
- 工具/模型/设备/额度加排序（用量、成本、名称、同步、剩余）
- EN/中切换，存在 `tm-dash-lang`
- 标题下「数据截至」取 Hub `updatedAt` 与各设备 `receivedAt` 的最新值
