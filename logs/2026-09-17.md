# 2026-09-17

## WorkBuddy Credits 没有进度条

网页把 `metric: credits` 当成纯金额行，跳过 `.bar`。Hub 已有 `remaining` / `usedPercent` / `remainingPercent` / `showMeter`；桌面 WorkBuddy/Trae 是金额 + 条。不改 fork。

- 大改前快照：`versions/2026-09-17-pre-credits-meter.html`
- credits 窗口：右边仍是剩余额度，`showMeter !== false` 且有百分比时画条
- `CREDITS` 格式改为裸数字（与桌面 `formatMoney` 一致，不再加 ` cr`）
- `spend` 仍无条

